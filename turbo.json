{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".vercel", "build", "dist"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
